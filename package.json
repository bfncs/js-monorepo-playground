{
  "name": "js-monorepo-playground",
  "private": true,
  "scripts": {
    "test": "jest --testPathPattern='modules/' --modulePaths ./modules --watch"
  },
  "devDependencies": {
    "babel-jest": "^21.2.0",
    "babel-preset-react-app": "^3.0.3",
    "jest": "^21.2.0",
    "prop-types": "^15.5.10",
    "react": "^15.6.1",
    "react-test-renderer": "15.6.1",
    "regenerator-runtime": "^0.11.0"
  },
  "babel": {
    "env": {
      "test": {
        "presets": [
          "react-app"
        ]
      }
    }
  }
}
